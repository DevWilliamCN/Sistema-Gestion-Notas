services:
  - type: web
    name: sistema-gestion-backend
    env: node
    buildCommand: cd server && npm install
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.1
    rootDir: server
